# Optional extras — install only if you want these features.
# GPU acceleration (NVIDIA CUDA):
#   pip install -r requirements-optional.txt --break-system-packages
#   (or run install.bat and answer "y" to the GPU prompt)
nvidia-cublas-cu12
nvidia-cudnn-cu12

# Speaker diarization (who-said-what labeling):
# Also requires a free HuggingFace account + accepting the model license at
# https://hf.co/pyannote/speaker-diarization-3.1
pyannote.audio
torch
